spring:
  datasource:
    url: jdbc:mysql://3.25.201.88:3306/classicmodels
    username: teamuser
    password: team123

  jpa:
    hibernate:
      ddl-auto: update   # Creates/updates tables automatically during development
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

server:
  port: 8080