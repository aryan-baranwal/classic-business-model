spring:
  datasource:
    url: jdbc:mysql://localhost:3306/classicmodel
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: none   # Do not create or update tables
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

server:
  port: 8080