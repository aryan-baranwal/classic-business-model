spring:
  datasource:
    url: jdbc:mysql://localhost:3306/classicmodel
    username: root
    password: root


  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

  thymeleaf:
    cache: false