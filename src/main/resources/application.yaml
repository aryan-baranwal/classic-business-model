spring:
  datasource:
    url: jdbc:mysql://localhost:3306/classicmodel
    username: root
    password: root


  jpa:
    hibernate:
      ddl-auto: update   # Creates/updates tables automatically during development
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

server:
  port: 8080