services:
  classicmodels-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: classicmodels-app:latest
    container_name: classicmodels-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
